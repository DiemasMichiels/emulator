{
  "name": "emulate",
  "displayName": "Emulate",
  "description": "Emulate Android and iOS easily!",
  "version": "0.0.1",
  "publisher": "michielsdiemas",
  "engines": {
    "vscode": "^1.22.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onCommand:extension.emulate"],
  "main": "./src/extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.emulate",
        "title": "Emulate"
      }
    ]
  },
  "scripts": {
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "vscode": "^1.1.6",
    "eslint": "^4.11.0"
  }
}
